# 1. Virtual Environments (Very heavy, don't upload)
.venv/
env/
venv/
ENV/

# 2. Python Cache (Autogenerated files that speed up execution)
**/__pycache__/
*.py[cod]

# 3. Environment Variables (Where you will hide your DB passwords)
.env

# 4. Data & DB (Don't upload the actual Postgres data or raw CSVs)
postgres_data/
*.csv
*.sqlite

# 5. MLflow Tracking (This folder gets huge with model artifacts)
mlruns/

# 6. IDE Settings (User-specific settings for VS Code/PyCharm)
.vscode/
.idea/